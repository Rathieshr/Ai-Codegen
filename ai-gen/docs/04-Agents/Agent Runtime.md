# Agent Runtime

Agent Runtime owns registration, event dispatch, scheduling, workflow state, retries, diagnostics, and approval checkpoints. It resolves services/skills dynamically and records every transition with a correlation ID.
