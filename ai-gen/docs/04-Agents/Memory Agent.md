# Memory Agent

The Memory Agent proposes Engineering Memory from approved or validated outcomes, detects duplicates, updates patterns through versioning, and archives obsolete evidence. It never indexes failed or rejected output.
