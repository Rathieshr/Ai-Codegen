# Repository Agent

The Repository Agent queues synchronization, creates snapshots, refreshes parsing/graph/ranking, publishes status, and retries recoverable failures. It does not modify repository content.
