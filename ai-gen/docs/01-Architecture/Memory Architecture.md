# Engineering Memory Architecture

Engineering Memory stores approved, versioned engineering knowledge derived from successful lifecycle outcomes.

`Validated outcome -> Draft memory -> Validation -> Approval -> Index -> Retrieval`

Memory supports planning, execution, and QA as ranked evidence. It never overrides current repository facts or planning boundaries. Raw prompts, failed outputs, rejected artifacts, stale unsupported claims, and unapproved work are excluded.
